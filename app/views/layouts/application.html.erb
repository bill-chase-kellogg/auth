<!DOCTYPE html>
<html>
  <head>
    <title>StarterApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <p>
      <!-- If Logged In -->
      
      <% if @current_user %>
      Welcome.
     <p><%= button_to "Log Out", "/sessions/logout", method: :delete %></p>
      <% else %>          
            
      <a href="/sessions/new">Log In</a>
      |
      <a href="/users/new">Sign Up</a>
    </p>
      <% end %>

    <p>
      <%= flash[:notice] %>
    </p>

    <%= yield %>
  </body>
</html>
